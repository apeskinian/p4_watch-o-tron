
// const deleteModal = new bootstrap.Modal('#delete-modal');


window.onload = () => {
    const editModal = new bootstrap.Modal('#edit-modal');
    if (editModal) {
        editModal.show();
    }
  }

